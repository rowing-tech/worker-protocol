{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "page.json",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {},
      "description": "ENDP-20. The page. The shared envelope cannot type its items — each surface's own schema narrows them."
    },
    "nextCursor": {
      "description": "ENDP-21. Opaque to the caller, produced only by the Worker, never constructed. A string because a caller sends it back as a request parameter. ENDP-20: absent at the end of the collection — absent, not null.",
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "items"
  ],
  "additionalProperties": false,
  "title": "Page",
  "description": "ENDP-20. The page envelope shared by every collection surface."
}
