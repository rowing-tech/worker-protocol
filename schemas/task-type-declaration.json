{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "task-type-declaration.json",
  "type": "object",
  "properties": {
    "payload": {
      "type": "object",
      "properties": {},
      "additionalProperties": {},
      "description": "TASK-32. The JSON Schema of this Task type's payload. The Worker's own — this protocol has no data model — and what a consumer renders or validates against."
    },
    "answeredBy": {
      "type": "string",
      "minLength": 1,
      "description": "TASK-32. The one Action of this Worker's own that answers a Task of this type, by the name its `actions` entry holds it under. One and not a list: where a Task can end several ways, the endings are variants of that Action's input, told apart by a discriminator. A name and not an instruction: the owner says what would answer, never who."
    }
  },
  "required": [
    "payload",
    "answeredBy"
  ],
  "additionalProperties": false,
  "title": "Task type declaration",
  "description": "TASK-32. One Task type, held under a qualified name (TASK-4, NAME-7) because it is matched by a party that did not mint it."
}
