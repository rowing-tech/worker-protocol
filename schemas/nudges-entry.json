{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "nudges-entry.json",
  "type": "object",
  "properties": {
    "version": {
      "type": "integer",
      "minimum": 1,
      "description": "DESC-9. A single integer; there is no minor version. DESC-29 makes it count breaking changes to this Capability's own surface alone."
    },
    "address": {
      "type": "string",
      "pattern": "^(?:https:\\/\\/|(?![A-Za-z][A-Za-z0-9+.-]*:))\\S*$",
      "title": "Address",
      "format": "uri-reference",
      "description": "DESC-12. An absolute https URL, or a relative reference resolved against the Descriptor's own URL. DESC-13 governs whether a credential may be sent to one that is off-origin."
    }
  },
  "required": [
    "version",
    "address"
  ],
  "additionalProperties": {},
  "title": "Nudges capability entry",
  "description": "NDG-1. The shared Capability entry with the address required. Nothing else: what arrives is fixed by `nudge.json` rather than declared, and which Task types this Worker will take one for is already its `skills` (NDG-3)."
}
