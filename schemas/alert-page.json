{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "alert-page.json",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "$ref": "alert.json"
      },
      "description": "ALRT-2. The Alerts whose conditions hold."
    },
    "nextCursor": {
      "description": "ENDP-21. Opaque to the caller, produced only by the Worker, never constructed. A string because a caller sends it back as a request parameter. ENDP-20: absent at the end of the collection — absent, not null.",
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "items"
  ],
  "additionalProperties": false,
  "title": "Alert page",
  "description": "ALRT-2. One page of Alerts, in the envelope ENDP-20 fixes for every collection."
}
