{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "metric-page.json",
  "type": "object",
  "properties": {
    "items": {
      "type": "array",
      "items": {
        "$ref": "metric-bucket.json"
      },
      "description": "MET-14. Ascending by start. MET-12: whole buckets only."
    },
    "nextCursor": {
      "description": "ENDP-21. Opaque to the caller, produced only by the Worker, never constructed. A string because a caller sends it back as a request parameter. ENDP-20: absent at the end of the collection — absent, not null.",
      "type": "string",
      "minLength": 1
    }
  },
  "required": [
    "items"
  ],
  "additionalProperties": false,
  "title": "Metric page",
  "description": "MET-14. One page of buckets, in the envelope ENDP-20 fixes for every collection in this protocol."
}
