{
  "schema": "tasks-entry",
  "valid": true,
  "rule": "TASK-27",
  "why": "One address, which a read answers Tasks from, and `raises` \u2014 what this Worker asks of others, and what the Tasks read at that address are instances of. What this Worker ANSWERS is not here: TASK-30 puts its Skill on the Descriptor root, because a Skill is served at no address. The entry declared a second address a claim was posted to until the Claim lifecycle was withdrawn.",
  "document": {
    "version": 1,
    "address": "../tasks",
    "raises": {
      "tech.rowing.fleet.verify-vehicle": {
        "payload": { "type": "object", "properties": { "vehicle": { "type": "string" } } },
        "answeredBy": ["record-verification"]
      }
    }
  }
}
