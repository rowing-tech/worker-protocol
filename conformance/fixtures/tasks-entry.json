{
  "schema": "tasks-entry",
  "valid": true,
  "rule": "TASK-27",
  "why": "One address, which a read answers Tasks from. `raises` is what this Worker asks of others and `answers` is its Skill, which is the unit the Tower catalogs by. The entry declared a second address a claim was posted to until the Claim lifecycle was withdrawn.",
  "document": {
    "version": 1,
    "address": "../tasks",
    "raises": {
      "tech.rowing.fleet.verify-vehicle": {
        "payload": { "type": "object", "properties": { "vehicle": { "type": "string" } } },
        "answeredBy": ["record-verification"]
      }
    },
    "answers": ["tech.rowing.pricing.price-a-quote"]
  }
}
