{
  "schema": "metrics-entry",
  "valid": true,
  "rule": "MET-2",
  "why": "The whole catalog of what a Worker publishes, a closed dimension beside a free one, and the calendar every bucket boundary is cut in. The surface itself never lists what exists — the Descriptor is the catalog, so a console knows the name, the unit and the periods before it asks for a number.",
  "document": {
    "version": 1,
    "address": "../metrics",
    "timeZone": "America/Montevideo",
    "metrics": {
      "tasks-resolved": {
        "unit": "tasks",
        "additive": true,
        "granularities": ["hour", "day", "week"],
        "dimensions": {
          "taskType": { "values": ["verify-vehicle", "price-quote"] },
          "tenant": {}
        }
      }
    }
  }
}
