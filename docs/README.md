# docs

The narrative: what this protocol is for, the architecture it assumes, and the reasoning that the
normative text leaves out.

Nothing here is normative. It is also where what is **deliberately undecided** is kept — the list
that invites argument, and where an open question lives until a `spec/` section can answer it.
