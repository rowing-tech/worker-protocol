{
  "name": "@worker-protocol/fleet-worker",
  "version": "0.0.0",
  "private": true,
  "description": "A Worker whose facts live in a Durable Object, run on workerd",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/worker.ts",
  "exports": {
    ".": "./src/worker.ts"
  },
  "scripts": {
    "typecheck": "wrangler types && tsc -p tsconfig.json",
    "test": "vitest run",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.22.0",
    "@types/node": "26.5.1",
    "typescript": "7.0.2",
    "vitest": "4.1.11",
    "wrangler": "4.131.1"
  },
  "dependencies": {
    "@worker-protocol/hono": "workspace:*",
    "hono": "4.13.7",
    "zod": "4.5.4"
  }
}
