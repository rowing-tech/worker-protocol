{
  "openapi": "3.1.0",
  "info": {
    "title": "worker-protocol — health",
    "version": "0.1",
    "description": "The answer to a poll: one status for the Worker and a map of named checks.",
    "license": {
      "name": "Apache-2.0",
      "identifier": "Apache-2.0"
    }
  },
  "servers": [
    {
      "url": "{address}",
      "description": "HLTH-1. The address the `health` entry declares, resolved per DESC-12 against the URL the Descriptor was read from.",
      "variables": {
        "address": {
          "default": "/",
          "description": "Read from the Worker's Descriptor and resolved per DESC-12. Never assembled by a caller (ENDP-1). The default is `/` and not a host on purpose: a client that was never given an address should fail at its own origin rather than reach somebody else's."
        }
      }
    }
  ],
  "paths": {
    "/": {
      "get": {
        "summary": "Poll health",
        "description": "HLTH-5. Answers `200` whatever it reports. The status is read from the body, and a response that is not `200` means the Worker did not answer rather than that it is unwell — which is the one distinction a health surface exists to draw.",
        "parameters": [
          {
            "name": "Worker-Protocol-Capability-Version",
            "in": "header",
            "required": false,
            "description": "ENDP-6. The Capability version the caller expects. A Worker that cannot answer it refuses the request whole with `400` and `unsupported_version`, and never substitutes its own.",
            "schema": {
              "type": "integer",
              "minimum": 1
            }
          }
        ],
        "responses": {
          "200": {
            "description": "HLTH-2. One status and a map of named checks.",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/health.json"
                }
              }
            }
          },
          "400": {
            "description": "ENDP-25. The shared error envelope, carrying one of: `unsupported_version` (ENDP-6).",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/error.json"
                }
              }
            }
          },
          "401": {
            "description": "ENDP-25. The shared error envelope, carrying one of: `unauthenticated` (REG-3).",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/error.json"
                }
              }
            }
          },
          "403": {
            "description": "ENDP-25. The shared error envelope, carrying one of: `forbidden` (ENDP-29).",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/error.json"
                }
              }
            }
          },
          "500": {
            "description": "ENDP-25. The shared error envelope, carrying one of: `internal_error` (ENDP-29).",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/error.json"
                }
              }
            }
          },
          "503": {
            "description": "ENDP-25. The shared error envelope, carrying one of: `unavailable` (ENDP-29).",
            "headers": {
              "Worker-Protocol-Edition": {
                "description": "ENDP-5. The edition that produced this answer. A caller that sees one it did not expect re-reads the Descriptor rather than parsing the body.",
                "required": true,
                "schema": {
                  "type": "string",
                  "pattern": "^(?:0|[1-9]\\d*)\\.(?:0|[1-9]\\d*)$"
                }
              },
              "Worker-Protocol-Capability-Version": {
                "description": "ENDP-5. The Capability version that produced this answer.",
                "required": true,
                "schema": {
                  "type": "integer",
                  "minimum": 1
                }
              }
            },
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "../schemas/error.json"
                }
              }
            }
          }
        }
      }
    }
  }
}
